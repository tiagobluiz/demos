spring:
  config:
    activate:
      on-profile: "development"
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:tc:postgresql:latest/////demo-test
  flyway:
    baseline-on-migrate: true